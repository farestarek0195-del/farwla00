import{useState}from'react';import{useNavigate}from'react-router-dom';export default function AdminLogin(){const nav=useNavigate();const[e,setE]=useState('');const[p,setP]=useState('');const submit=x=>{x.preventDefault();if(e==='admin@frawla.com'&&p==='admin123'){localStorage.setItem('frawla-admin','1');nav('/admin')}else alert('بيانات الدخول غير صحيحة')};return <div className="admin-login"><form className="form-card" onSubmit={submit}><div className="logo">Frawla</div><h1>Admin Login</h1><p>Demo: admin@frawla.com / admin123</p><input className="input" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input className="input" placeholder="Password" type="password" value={p} onChange={x=>setP(x.target.value)}/><button className="btn btn-primary full">Login</button></form></div>}
