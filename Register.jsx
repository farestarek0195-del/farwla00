import{useState}from'react';import{useNavigate}from'react-router-dom';export default function Register(){const nav=useNavigate();const[name,setName]=useState('');const[email,setEmail]=useState('');const submit=e=>{e.preventDefault();localStorage.setItem('frawla-user',JSON.stringify({name,email}));nav('/profile')};return <section className="section"><div className="container narrow"><div className="page-title"><span className="eyebrow">JOIN FRAWLA</span><h1>Create account</h1></div><form className="form-card" onSubmit={submit}><label className="label">Name<input required className="input" value={name} onChange={e=>setName(e.target.value)}/></label><label className="label">Email<input required type="email" className="input" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="label">Password<input required type="password" className="input"/></label><button className="btn btn-primary full">Create account</button></form></div></section>}
