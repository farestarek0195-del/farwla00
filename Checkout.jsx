import{useState}from'react';import{useNavigate}from'react-router-dom';import{useStore}from'../context/StoreContext';export default function Checkout(){const{cart}=useStore();const nav=useNavigate();const[submitting,setSubmitting]=useState(false);const[form,setForm]=useState({name:'',phone:'',address:'',city:'',payment:'cod'});const total=cart.reduce((s,x)=>s+x.price*x.quantity,0)+(cart.reduce((s,x)=>s+x.price*x.quantity,0)>=1000?0:60);const submit=e=>{e.preventDefault();if(!cart.length)return;setSubmitting(true);const orders=JSON.parse(localStorage.getItem('frawla-orders')||'[]');orders.unshift({id:'FR-'+Date.now().toString().slice(-6),customer:form.name,phone:form.phone,address:form.address,city:form.city,payment:form.payment,total,status:'Pending',items:cart,date:new Date().toLocaleString('en-EG')});localStorage.setItem('frawla-orders',JSON.stringify(orders));localStorage.removeItem('frawla-cart');setTimeout(()=>nav('/orders'),300)};return <section className="section"><div className="container"><div className="page-title"><span className="eyebrow">CHECKOUT</span><h1>Complete your order</h1></div><form className="form-card" onSubmit={submit}><label className="label">الاسم<input required className="input" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="label">رقم الهاتف<input required className="input" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label className="label">المحافظة<input required className="input" value={form.city} onChange={e=>setForm({...form,city:e.target.value})}/></label><label className="label">العنوان<textarea required className="input" rows="4" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label><label className="label">طريقة الدفع<select className="input" value={form.payment} onChange={e=>setForm({...form,payment:e.target.value})}><option value="cod">الدفع عند الاستلام</option><option value="online">دفع إلكتروني</option></select></label><div className="checkout-total">الإجمالي: <strong>{total} EGP</strong></div><button disabled={submitting} className="btn btn-primary full">{submitting?'جاري التنفيذ...':'تأكيد الطلب'}</button></form></div></section>}
