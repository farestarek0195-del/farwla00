import{useState}from'react';import{useNavigate,Link}from'react-router-dom';export default function Login(){const nav=useNavigate();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const submit=e=>{e.preventDefault();localStorage.setItem('frawla-user',JSON.stringify({email}));nav('/profile')};return <section className="section"><div className="container narrow"><div className="page-title"><span className="eyebrow">WELCOME BACK</span><h1>Login</h1></div><form className="form-card" onSubmit={submit}><label className="label">Email<input required type="email" className="input" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="label">Password<input required type="password" className="input" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn btn-primary full">Login</button><p>New here? <Link to="/register">Create account</Link></p></form></div></section>}
