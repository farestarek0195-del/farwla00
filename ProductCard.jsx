import{Heart,ShoppingBag}from'lucide-react';import{Link}from'react-router-dom';import{useStore}from'../context/StoreContext';export default function ProductCard({product}){const{addToCart,wishlist,toggleWishlist}=useStore();const liked=wishlist.some(x=>x.id===product.id);return <article className="product-card"><button className="heart" onClick={()=>toggleWishlist(product)}><Heart size={18} fill={liked?'currentColor':'none'}/></button><Link to={'/product/'+product.id}><img className="product-image" src={product.image} alt={product.name}/></Link><div className="product-body"><div className="product-meta">{product.category}</div><h3 className="product-name"><Link to={'/product/'+product.id}>{product.name}</Link></h3><div className="product-row"><div><span className="price">{product.price} EGP</span>{product.oldPrice>0&&<span className="old-price">{product.oldPrice}</span>}</div><button className="icon-btn" onClick={()=>addToCart(product)}><ShoppingBag size={18}/></button></div></div></article>}
