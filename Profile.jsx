import{Link,useNavigate}from'react-router-dom';export default function Profile(){const nav=useNavigate();const u=JSON.parse(localStorage.getItem('frawla-user')||'null');return <section className="section"><div className="container narrow"><div className="page-title"><span className="eyebrow">MY ACCOUNT</span><h1>Profile</h1></div><div className="form-card">{u?<><h2>{u.name||'Welcome'}</h2><p>{u.email}</p><Link className="btn btn-primary" to="/orders">My Orders</Link><button className="btn btn-light" onClick={()=>{localStorage.removeItem('frawla-user');nav('/')}}>Logout</button></>:<><p>سجل دخولك الأول.</p><Link className="btn btn-primary" to="/login">Login</Link></>}</div></div></section>}
