export default function AdminCustomers(){const orders=JSON.parse(localStorage.getItem('frawla-orders')||'[]');const map=new Map();orders.forEach(o=>map.set(o.phone||o.customer,{name:o.customer,phone:o.phone}));return <div><div className="admin-head"><h1>Customers</h1></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Phone</th></tr></thead><tbody>{[...map.values()].map((c,i)=><tr key={i}><td>{c.name}</td><td>{c.phone}</td></tr>)}</tbody></table></div></div>}
