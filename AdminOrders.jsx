import{useState}from'react';export default function AdminOrders(){const[orders,setOrders]=useState(()=>JSON.parse(localStorage.getItem('frawla-orders')||'[]'));const update=(id,status)=>{const n=orders.map(o=>o.id===id?{...o,status}:o);setOrders(n);localStorage.setItem('frawla-orders',JSON.stringify(n))};return <div><div className="admin-head"><h1>Orders</h1></div><div className="table-wrap"><table><thead><tr><th>ID</th><th>Customer</th><th>Total</th><th>Status</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.id}</td><td>{o.customer}<br/>{o.phone}</td><td>{o.total} EGP</td><td><select className="input" value={o.status} onChange={e=>update(o.id,e.target.value)}>{['Pending','Confirmed','Processing','Shipped','Completed','Cancelled'].map(s=><option key={s}>{s}</option>)}</select></td></tr>)}</tbody></table></div></div>}
