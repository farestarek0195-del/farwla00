import{Link}from'react-router-dom';import ProductCard from'../components/ProductCard';import{useStore}from'../context/StoreContext';export default function Wishlist(){const{wishlist}=useStore();return <section className="section"><div className="container"><div className="page-title"><span className="eyebrow">SAVED</span><h1>Wishlist</h1></div>{!wishlist.length?<div className="empty"><h2>مفيش منتجات محفوظة</h2><Link to="/shop" className="btn btn-primary">Shop now</Link></div>:<div className="grid grid-4">{wishlist.map(p=><ProductCard key={p.id} product={p}/>)}</div>}</div></section>}
