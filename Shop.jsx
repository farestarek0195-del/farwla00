import{useParams}from'react-router-dom';import{useMemo,useState}from'react';import ProductCard from'../components/ProductCard';import{useStore}from'../context/StoreContext';export default function Shop(){const{category}=useParams();const{products}=useStore();const[q,setQ]=useState('');const list=useMemo(()=>products.filter(p=>(!category||p.category===category)&&((p.name+' '+p.nameAr).toLowerCase().includes(q.toLowerCase()))),[products,category,q]);return <section className="section"><div className="container"><div className="page-title"><span className="eyebrow">FRAWLA SHOP</span><h1>{category?category.toUpperCase():'كل المنتجات'}</h1><p>{list.length} products</p></div><input className="input search" value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحثي عن منتج..."/><div className="grid grid-4">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div></div></section>}
